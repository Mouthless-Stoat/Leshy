# Leshy

A framework to create rule engine for Inscryption

# Development

If you have nix install and flakes enable, just run:

```
nix develop
```

This will setup everything rust for you. The command will install the compiler, cargo, the LSP and formatter.
