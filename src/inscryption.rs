use crate::{FightManager, SigilEvent, SigilHandler};

#[derive(Clone, Debug)]
enum Sigil {
    BifucatedStrike,
    BoneKing,
    BombLatch,
    Amorphorus,
    RabbitHole,
    DamBuilder,
    BeesWithin,
    LooseTails,
    Sprinter,
    Waterborne,
    Guardian,
    Burrower,
}

struct Handler;
